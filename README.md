#1 Clone Project

#2 Move to the project folder

#3 run the following command in cmd: 
  - For dev: npm run docker:dev
  - For prod: npm run docker:prod
  - Exit and Remove: npm run docker:exit

  ** See the package.json file for more information. **  

#4 access page: http://localhost:3000