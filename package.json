{
  "name": "test-1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.override.dev.yml up --build",
    "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose.override.prod.yml up --build",
    "docker:exit": "docker compose down"
  },
  "author": "",
  "license": "ISC"
}
